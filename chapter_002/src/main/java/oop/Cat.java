package oop;
/**
 * 1.1. Создание объекта[#234849]
 * Здесь мы создаем переменную типа Cat с именем peppy(sparky). Далее мы записываем в эту переменную объект типа Cat.
 * Важный момент. Оператор new резервирует кусок памяти и записывает в нее данные объекта класса Cat.
 */
public class Cat {
    public static void main(String[] args) {
        Cat peppy = new Cat();
        Cat sparky = new Cat();
    }
}
